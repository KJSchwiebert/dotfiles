# MATLAB Config

To use this config for MATLAB simply:
1. Open MATLAB and run `userpath`.
2. `ln -s ~/dotfiles/MATLAB/startup.m "$userpath"/startup.m`.
3. `ln -s ~'dotfiles/MATLAB/overrides "$userpath"/overrides'`
4. Restart MATLAB for the changes to take effect.
